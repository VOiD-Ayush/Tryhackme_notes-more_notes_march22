# This will be the test readme

> learing command line


```bash
git status
# Shows all the untracked files
# it shows red in untracked files

git add filename
or 
git add .
# tracks the filename or all files in the directory
# it shows green in tracked files

git commit -m "Adding stuff to test" -m "This is discription"

# commits the files locally

git push 
# pushed code online on github

git push origin main
# origin is where to push
# master is branch
```